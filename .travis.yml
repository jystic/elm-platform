language: objective-c

install:
  - brew update
  - brew install ghc
  - brew install cabal-install
  - cabal update
  - git clone -b osx https://github.com/bmjames/hs-certificate
  - (cd hs-certificate/x509-system && cabal install)

script:
  - installers/mac/make-installer.sh
